#include "OpenGLVertexBuffer.h"
namespace Cast{


    // void OpenGLVertexBuffer::init(uint32_t drawType){
    //     glGenBuffers(1, &this->_id);  
    //     glBindBuffer(GL_ARRAY_BUFFER, this->_id);  
    //     CAST_ASSERT(_buffer.size() > 0);
    //     glBufferData(GL_ARRAY_BUFFER, sizeof(_buffer[0]) * _buffer.size(), _buffer, drawType);
    // }


}
